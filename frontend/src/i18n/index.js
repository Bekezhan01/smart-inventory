import ru from './ru';

// Default language is Russian.
// To add more languages: import and add to `languages` map,
// then read the user preference from localStorage/context.
export const t = ru;
export default ru;
