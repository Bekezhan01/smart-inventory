{
  "name": "smart-inventory-backend",
  "version": "2.0.0",
  "description": "Smart Inventory Management System - Backend API with RBAC + WebAuthn",
  "main": "src/server.js",
  "scripts": {
    "start": "npm run start:prod",
    "start:prod": "npm run bootstrap && node src/server.js",
    "start:raw": "node src/server.js",
    "bootstrap": "npm run db:deploy && npm run db:seed",
    "dev": "nodemon src/server.js",
    "db:migrate": "npx prisma migrate dev",
    "db:deploy": "npx prisma migrate deploy",
    "db:push": "npx prisma db push",
    "db:seed": "node src/utils/seed.js",
    "db:generate": "npx prisma generate",
    "db:studio": "npx prisma studio"
  },
  "dependencies": {
    "@prisma/client": "^6.19.3",
    "@simplewebauthn/server": "^9.0.3",
    "bcrypt": "^6.0.0",
    "bcryptjs": "^2.4.3",
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "express": "^4.18.2",
    "express-rate-limit": "^7.2.0",
    "express-validator": "^7.0.1",
    "helmet": "^7.1.0",
    "jsonwebtoken": "^9.0.2",
    "morgan": "^1.10.0",
    "prisma": "^6.19.3"
  },
  "devDependencies": {
    "nodemon": "^3.1.0"
  }
}
